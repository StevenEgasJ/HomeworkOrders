{
  "name": "market-tatylu-mvc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:server": "node --watch controllers/server.js",
    "dev:client": "npm run dev --prefix views",
    "start": "node controllers/server.js",
    "server:install": "npm install --production",
    "client:build": "npm install --prefix views --production=false && npm run build --prefix views",
    "seed": "node scripts/seedUser.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.19.2",
    "mongoose": "^7.6.1",
    "morgan": "^1.10.0"
  }
}
